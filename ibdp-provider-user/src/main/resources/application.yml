server:
  port: 8006

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
  instance:
    instance-id: ibdp-user
    prefer-ip-address: true
spring:
  application:
    name: ibdp-user

info:
  app.name: ibdp
  build.artifactId: $project.artifactId$
  build.version: $project.version$

